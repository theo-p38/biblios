{% extends 'base.html.twig' %}

{% block title %}Hello AuthorController!{% endblock %}

{% block body %}
    <br/>
    <div class="w-50 m-auto mt-5">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary m-auto d-block mb-3">Envoyer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
